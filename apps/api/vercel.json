{
    "version": 2,
    "framework": null,
    "buildCommand": "cd ../.. && pnpm prisma generate --schema=prisma/schema.prisma && pnpm exec turbo run build",
    "outputDirectory": "dist",
    "functions": {
        "api/index.js": {
            "includeFiles": "dist/**"
        }
    },
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/api"
        }
    ],
    "crons": [
        {
            "path": "/api/v1/cron/worker",
            "schedule": "0 0 * * *"
        }
    ]
}
