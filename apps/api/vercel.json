{
    "version": 2,
    "framework": null,
    "installCommand": "cd ../.. && pnpm install --prod=false",
    "buildCommand": "cd ../.. && pnpm prisma generate --schema=prisma/schema.prisma && pnpm turbo run build --filter=@bookpro/api...",
    "outputDirectory": "dist",
    "functions": {
        "api/index.js": {
            "includeFiles": "dist/**"
        }
    },
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/api"
        }
    ],
    "crons": [
        {
            "path": "/api/v1/cron/worker",
            "schedule": "* * * * *"
        }
    ]
}
